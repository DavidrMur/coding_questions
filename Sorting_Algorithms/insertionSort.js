function insertionSort(arr) {

    let i = 0;
    let j = 1;

    while (i < arr.length) {
        // sort this shat
        let value = arr[i+1];
        for (let j =0; j < i; j++) {
            if (value < arr[j]) arr.pre
        }
    }

}

console.log(insertionSort[5,4,3,2,1]);